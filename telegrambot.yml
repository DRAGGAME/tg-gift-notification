name: TelegramBotGift
channels:
  - conda-forge
  - defaults
dependencies:
  - pip>=21.0.1
  - setuptools>=51.00.0
  - python>=3.10.0
  - asyncpg
  - apscheduler
  -
  - pip:
    - aiogram>=3.5.0
    - python-dotenv
